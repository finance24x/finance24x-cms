{
  "collectionName": "components_section_grid_item_with_dates",
  "info": {
    "displayName": "Grid Item with Date",
    "icon": "calendar",
    "description": "Item for grid-type sections with date display (events, IPOs, etc.)"
  },
  "options": {},
  "attributes": {
    "image": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": ["images"]
    },
    "date": {
      "type": "date",
      "required": true,
      "description": "Event/IPO date"
    },
    "title": {
      "type": "string",
      "required": true
    },
    "icon": {
      "type": "string",
      "required": false,
      "description": "FontAwesome icon class (e.g., 'fa-calendar')"
    },
    "iconText": {
      "type": "string",
      "required": false,
      "description": "Text next to icon (e.g., 'Upcoming', 'Live')"
    },
    "description": {
      "type": "text",
      "required": false
    },
    "url": {
      "type": "string",
      "required": true
    },
    "publishedDate": {
      "type": "date",
      "required": true
    }
  }
}

